# Q-Learning UFT离散化器 - 修改总结

## 一、修改思路

### 核心问题
原有的 `get_uft_flag()` 函数使用**固定的数学变换**（对数映射）将连续的UFT值离散化为0-63的分组ID。这种方法存在以下问题：

1. **非自适应** - 对数变换的分组边界固定，无法根据实际数据分布调整
2. **信息损失** - 固定的对数间隔可能导致某些区间信息丢失
3. **工作负载不敏感** - 无法应对不同的UFT值分布特性

### 改进思路
采用**动态离散化器**替代固定的数学变换：

1. **多分组架构** - 最多8个分组，每个分组独立维护min/max值
2. **自适应分裂** - 根据实际数据分布动态调整分组边界
3. **几何平均分裂** - 使用几何平均而非算术平均计算分裂点，更适应指数分布

## 二、核心设计

### 1. 数据结构

```c
struct {
    struct {
        s64 min_val;        /* 该分组的最小值 */
        s64 max_val;        /* 该分组的最大值 */
        u32 sample_count;   /* 该分组的样本数 */
    } groups[8];
    u32 group_count;        /* 活跃分组数（初始为1） */
    u32 total_samples;      /* 总采样次数 */
} uft_discretizer;
```

**特点**：
- 预分配8个分组（固定大小，无动态分配）
- 每个分组记录自己的统计信息，完全独立
- 初始状态：1个分组，范围为[LLONG_MAX, LLONG_MIN]

### 2. 几何平均函数

```c
static inline s64 geometric_mean(s64 a, s64 b) {
    /* 几何平均 = sqrt(a*b) ≈ 2^((log2(a) + log2(b))/2) */
    /* 纯整数运算，避免浮点数 */
}
```

**计算流程**：
1. 使用 `fixed_log2()` 计算两数的对数
2. 求对数的平均值
3. 用指数函数转换回线性空间
4. 使用定点运算近似 2^(log_avg)

### 3. 离散化函数

```c
unsigned discretize_uft_value(s64 uft_value)
```

**工作流程**（下文详述）

## 三、运行流程

### 阶段1：初始化（第1个样本）
```
输入：UFT值 = 100

1. 检查 total_samples == 0 → TRUE
2. 初始化第一个分组：
   - groups[0].min_val = 100
   - groups[0].max_val = 100
   - groups[0].sample_count = 1
3. group_count = 1
4. total_samples = 1
5. 返回 group_id = 0
```

### 阶段2：常规更新（第2-100个样本）
```
输入：UFT值 = 150

1. total_samples++ → 2
2. 线性查找找到所属分组：
   - 遍历所有分组，找到值应属的分组
   - 示例：150在[100, 100]之间？否
   - 选择best_group = 0（扩展该分组）
3. 更新分组范围：
   - groups[0].min_val = min(100, 150) = 100
   - groups[0].max_val = max(100, 150) = 150
   - groups[0].sample_count = 2
4. 检查分裂条件：
   - sample_count (2) >= 100？否 → 不分裂
5. 映射到输出范围：
   - return (0 * 64 / 1) % 64 = 0
```

### 阶段3：分裂触发（第100+个样本，且违反条件）
```
输入：UFT值 = 300
当前状态：groups[0] = {min:100, max:250, count:100}

1. total_samples++ → 101
2. 线性查找：best_group = 0
3. 更新分组范围：
   - groups[0].max_val = max(250, 300) = 300
   - groups[0].sample_count = 101
4. 检查分裂条件：
   ✓ sample_count (101) >= 100？是
   ✓ group_count (1) < 8？是
   ✓ min_val (100) > 0？是
   ✓ max_val (300) >= 2*min_val (200)？是 → 违反条件！

5. 执行分裂：
   - 计算分裂点：split_point = geometric_mean(100, 300)
     ≈ sqrt(100 * 300) ≈ 173
   
   - 创建新分组：
     groups[1].min_val = 173
     groups[1].max_val = 300
     groups[1].sample_count = 0
   
   - 调整原分组：
     groups[0].max_val = 173
   
   - 更新计数：
     group_count = 2

6. 映射到输出范围：
   - return (0 * 64 / 2) % 64 = 0
```

### 阶段4：后续样本分配到新分组
```
输入：UFT值 = 250

1. 线性查找找到所属分组：
   - 检查 250 是否在 [100, 173]？否
   - 检查 250 是否在 [173, 300]？是
   - best_group = 1

2. 更新分组1的范围：
   - groups[1].min_val = min(173, 250) = 173
   - groups[1].max_val = max(300, 250) = 300
   - groups[1].sample_count = 1

3. 映射到输出范围：
   - return (1 * 64 / 2) % 64 = 32
```

## 四、关键特性

### 1. 自适应性
- **动态边界** - 分组边界根据数据实时调整
- **样本驱动** - 需要100个样本才能考虑分裂，确保统计有效性
- **逐步细分** - 最多分裂到8个分组，防止过度细分

### 2. 正确性
- **独立分组** - 每个分组的min/max独立维护，互不干扰
- **约束检查** - 确保每个分组满足 `max < 2*min` 的条件
- **边界保护** - 分裂点永不等于分组边界，防止无效分裂

### 3. 效率
- **线性查找** - O(n)复杂度，n≤8，实际上接近O(1)
- **预分配内存** - 8个分组固定大小，无运行时分配
- **整数运算** - 几何平均使用定点算术，无浮点数开销

### 4. 鲁棒性
- **零初始化** - 结构体中所有字段都有默认值
- **范围检查** - split_point的有效范围检查
- **溢出防护** - 几何平均计算中的溢出检查

## 五、执行时间线

### 启动时
1. 内核加载 yaffs_guts.c
2. 全局变量 `uft_discretizer` 初始化
3. group_count = 1, total_samples = 0

### 第1个GC操作
1. 调用 `get_usable_state(tags, aver_uft)`
2. 计算UFT值，调用 `get_uft_flag(uft, aver_uft)`
3. 进入 `discretize_uft_value(uft)`
4. **初始化分组0**，返回状态ID=0
5. 状态进入Q-table

### 第2-100个GC操作
1. 类似流程，但每个值都**扩展分组0的范围**
2. 累积样本直到达到100个

### 第101个GC操作（假设满足分裂条件）
1. 样本数 = 101，分组范围超过2倍
2. **触发分裂**：创建分组1
3. 后续值会被分配到不同分组
4. Q-table中的状态更加细粒度

### 长期运行
1. 继续采集样本，可能再次分裂
2. 最终形成2-8个稳定分组
3. **状态编码变成自适应的**，而非固定的对数映射

## 六、对Q-Learning的影响

### 原有方式（固定对数映射）
```
UFT值 50  → 固定映射 → 状态ID 20
UFT值 100 → 固定映射 → 状态ID 25
UFT值 200 → 固定映射 → 状态ID 30
（间隔均匀）
```

### 新方式（自适应离散化）
```
假设实际数据分布为：50(5个), 100(10个), 200(30个)

阶段1：
状态ID：50→0, 100→0, 200→0（全在同一分组）

阶段2（分裂后）：
状态ID：50→0, 100→0, 200→32（分到不同分组）

优势：
✓ 自动适应数据分布
✓ 高频数据区间更细
✓ 低频数据区间更粗
✓ Q-Learning获得更好的特征表示
```

## 七、性能指标

| 指标 | 值 |
|------|-----|
| 最多分组数 | 8 |
| 分裂触发样本数 | 100 |
| 几何平均计算时间 | O(1)（固定操作） |
| 线性查找时间 | O(n), n≤8 |
| 内存占用 | ~512 bytes（8个分组×64 bytes） |
| 是否使用浮点数 | 否（纯整数） |

