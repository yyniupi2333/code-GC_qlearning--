# 分组策略在Q-Learning流程中的演化

## 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│  全局离散化器 (Global UFT Discretizer)                          │
├─────────────────────────────────────────────────────────────────┤
│ 目标：将连续的UFT值（0 ~ 无穷）映射到64个离散状态（0~63）     │
│ 优势：适应性强，根据实际数据分布自动调整分组                  │
│ 约束：最多64个分组（Q表状态数），进程内全局维护               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 第一部分：初始化阶段（Sample 1）

### 时间点：系统启动 + 第一个有效页处理

```c
/* 数据结构初始化状态 */
uft_discretizer = {
    .total_samples = 0,
    .group_count = 1,  ← 预分配1个分组
    .groups[0] = {
        .min_val = LLONG_MAX,   // 未触及
        .max_val = LLONG_MIN,   // 未触及
        .sample_count = 0       // 无样本
    },
    .groups[1..63] = {
        .min_val = LLONG_MAX,
        .max_val = LLONG_MIN,
        .sample_count = 0
    }
}
```

### 第一个样本到达

**情景**：处理受害块中的第一个有效页

```
时间轴：
├─ T=1000ms，页创建，obj_id=100, chunk_id=5
├─ T=2000ms，数据最后访问
├─ T=8000ms，触发GC处理该页
│
└─ UFT = (8000 - 2000) / 1 = 6000

离散化器状态变化：
┌─────────────────────────────────────────────┐
│ BEFORE：                                    │
│ ├─ group_count = 1 (未使用)                │
│ ├─ total_samples = 0                       │
│ └─ groups[0] = [∞, -∞, 0]                 │
├─────────────────────────────────────────────┤
│ AFTER：                                     │
│ ├─ group_count = 1 (激活)                  │
│ ├─ total_samples = 1                       │
│ └─ groups[0] = [6000, 6000, 1]    ← 单点  │
│                  min    max    cnt         │
└─────────────────────────────────────────────┘

返回状态ID = 0   ← 所有UFT值归入同一分组
```

**关键逻辑**：

```c
if (uft_discretizer.total_samples == 0) {
    uft_discretizer.groups[0].min_val = 6000;
    uft_discretizer.groups[0].max_val = 6000;
    uft_discretizer.groups[0].sample_count = 1;
    uft_discretizer.total_samples = 1;
    return 0;  ← 第一个样本总是分组0
}
```

---

## 第二部分：样本积累阶段（Samples 2~99）

### 时间点：处理第2到第99个不同UFT值的页面

**场景**：收集不同热度的数据

```
时间序列（简化示例）：
  GC #2 → UFT=5500  → 归入分组0 → groups[0]=[5500, 6000, 2]
  GC #3 → UFT=6200  → 归入分组0 → groups[0]=[5500, 6200, 3]
  GC #4 → UFT=10000 → 归入分组0 → groups[0]=[5500, 10000, 4]
  GC #5 → UFT=100   → 归入分组0 → groups[0]=[100, 10000, 5]
  ...
  GC #99 → UFT=8000 → 归入分组0 → groups[0]=[100, 18000, 99]

```

### 分组状态演化

```
┌──────────────────────────────────────────────────────────┐
│ 积累阶段的分组变化（Sample 2~99）                        │
├──────────────────────────────────────────────────────────┤
│ Sample  │ UFT值  │ groups[0].range      │ 范围扩展情况    │
├──────────────────────────────────────────────────────────┤
│ 1       │ 6000  │ [6000, 6000]         │ 初始化          │
│ 2       │ 5500  │ [5500, 6000]         │ 扩展最小值      │
│ 3       │ 6200  │ [5500, 6200]         │ 扩展最大值      │
│ 10      │ 100   │ [100, 6200]          │ 大幅扩展最小值  │
│ 50      │ 18000 │ [100, 18000]         │ 扩展最大值      │
│ 99      │ 8000  │ [100, 18000]         │ 在范围内        │
└──────────────────────────────────────────────────────────┘

分组内部统计：
groups[0] = {
    .min_val = 100,
    .max_val = 18000,
    .sample_count = 99,
    
    状态特征：
    ├─ 范围跨度：18000 / 100 = 180倍
    ├─ 违反条件检查：max(18000) >= 2 * min(100) ?
    │  18000 >= 200 ? YES → 违反max < 2*min条件
    └─ 但条件2未满足：sample_count(99) < 100
       ⟹ 不执行分裂，继续积累
```

### 关键特点

- **线性查找**：每个样本在 O(group_count) 时间内找到所属分组
- **范围扩展**：分组范围不断扩大，包容更多样本
- **无分裂触发**：因为样本数还不足100，即使违反条件也不分裂
- **单一分组**：目前仍然只有1个活跃分组

---

## 第三部分：临界分裂阶段（Sample 100~200）

### 时间点：GC循环中第100次处理

**关键决策点**：分裂条件全部满足

```
分裂前置条件检查（第100个样本）：
✓ 条件1：sample_count >= 100         → 99+1=100 ✓
✓ 条件2：max >= 2*min                → 18000 >= 200 ✓
✓ 条件3：group_count < 64            → 1 < 64 ✓
✓ 条件4：min_val > 0                 → 100 > 0 ✓

分裂执行！
```

### 分裂点计算（几何平均）

```
几何平均分裂策略：
┌────────────────────────────────────────────────┐
│ 目标：找到最优的分裂点                         │
│ 原理：使用几何平均，避免偏向极值              │
└────────────────────────────────────────────────┘

Input:
  min_val = 100
  max_val = 18000

计算过程：
  Step 1: 计算对数
    log2(100) = 6.644
    log2(18000) = 14.137
  
  Step 2: 对数平均
    (6.644 + 14.137) / 2 = 10.391
  
  Step 3: 转换回线性空间
    split_point = 2^10.391 ≈ 1346
  
  几何平均 = sqrt(100 * 18000) = sqrt(1800000) ≈ 1342

验证分裂点有效性：
  ✓ split_point(1346) > min_val(100)
  ✓ split_point(1346) < max_val(18000)
  ✓ [100, 1346] 和 [1346, 18000] 都满足max < 2*min
```

### 分裂执行

```c
/* 分裂前状态 */
uft_discretizer = {
    .group_count = 1,
    .total_samples = 100,
    .groups[0] = {[100, 18000, 100]}
}

/* 执行分裂 */
split_point = geometric_mean(100, 18000) = 1346;

/* 创建新分组 */
u32 new_group_idx = 1;
new_group = &uft_discretizer.groups[1];
new_group->min_val = 1346;      // [1346, 18000]
new_group->max_val = 18000;
new_group->sample_count = 0;

/* 旧分组调整 */
group->max_val = 1346;           // [100, 1346]

uft_discretizer.group_count = 2;

/* 分裂后状态 */
uft_discretizer = {
    .group_count = 2,  ← 增加1个分组
    .groups[0] = {[100, 1346, 100]}     ← 低温数据（冷数据）
    .groups[1] = {[1346, 18000, 0]}     ← 新分组（中温/热数据）
}
```

### 分裂直观图示

```
分裂前（1个分组）：
┌──────────────────────────────────────┐
│         Group 0: [100, 18000]        │
│  ├─冷数据(100~1346)                 │
│  ├─中温数据(1346~5000)              │
│  ├─热数据(5000~18000)               │
│  │                                  │
│  └─状态ID都是0（精度低）             │
└──────────────────────────────────────┘
  样本分布太广，无法区分数据温度

分裂后（2个分组）：
┌─────────────────┬────────────────────┐
│   Group 0       │    Group 1         │
│  [100, 1346]    │  [1346, 18000]     │
│                 │                    │
│  冷数据→状态ID:0 │ 热/中温数据→状态ID:1 │
│  精度：2倍提升   │                    │
└─────────────────┴────────────────────┘
```

---

## 第四部分：连续分裂阶段（Samples 200~50000）

### 第二次分裂（Sample 200+）

```
触发条件（Group 1）：
  ├─ sample_count >= 100 ✓
  ├─ max >= 2*min:  18000 >= 2*1346? → 18000 >= 2692 ✓
  └─ group_count < 64 ✓

分裂执行：
  split_point = geometric_mean(1346, 18000) = geometric_mean ≈ 4928
  
  结果：
    Group 1 → [1346, 4928] (中温数据)
    Group 2 → [4928, 18000] (热数据)
    group_count = 3
```

### 递归分裂过程

```
┌─────────────────────────────────────────────────────────────────┐
│ 分裂演化树                                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 第1阶段 (Sample 100):                                          │
│    Group-0 ─────────→ [分裂] ─────→ Group-0 + Group-1        │
│    [100, 18000]              split@1346                      │
│                                                                 │
│ 第2阶段 (Sample 200):                                          │
│    Group-1 ─────────→ [分裂] ─────→ Group-1 + Group-2        │
│    [1346, 18000]            split@4928                       │
│                                                                 │
│ 第3阶段 (Sample 300):                                          │
│    Group-2 ─────────→ [分裂] ─────→ Group-2 + Group-3        │
│    [4928, 18000]            split@9343                       │
│                                                                 │
│ ... 继续分裂直到：                                             │
│    1. 达到64个分组上限                                        │
│    2. 所有分组都满足 max < 2*min                              │
│                                                                 │
│ 最终状态：                                                     │
│    Group-0: [100, 1346]      → 状态ID=0  (冷数据)            │
│    Group-1: [1346, 4928]     → 状态ID=1  (中温)              │
│    Group-2: [4928, 9343]     → 状态ID=2  (中温偏热)          │
│    ...                                                         │
│    Group-N: [..., max_uft]   → 状态ID=N  (热数据)            │
│    (group_count ≤ 64)                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 分裂频率与时间表

```
时间序列估算（假设每次GC处理约100~200个样本）：

┌─────────────┬────────────┬─────────────┬──────────────┐
│ GC循环      │ 样本总数   │ 分组数      │ 备注         │
├─────────────┼────────────┼─────────────┼──────────────┤
│ 0~100       │ 0~100      │ 1           │ 积累阶段     │
│ 100~200     │ 100~200    │ 2           │ 第1次分裂    │
│ 200~300     │ 200~300    │ 3           │ 第2次分裂    │
│ 300~500     │ 300~500    │ 5           │ 快速分裂     │
│ 500~1000    │ 500~1000   │ 10          │ 加速期       │
│ 1000~5000   │ 1K~5K      │ 20~40       │ 高速期       │
│ 5000~20000  │ 5K~20K     │ 40~64       │ 接近饱和     │
│ 20000~50000 │ 20K~50K    │ 64(饱和)    │ 维持期       │
└─────────────┴────────────┴─────────────┴──────────────┘
```

---

## 第五部分：样本分配机制

### 样本归入算法

```c
/* 线性查找：找到值应该属于的分组 */
for (i = 0; i < group_count; i++) {
    s64 group_min = groups[i].min_val;
    s64 group_max = groups[i].max_val;
    
    /* 情况1：值在范围内（闭包） */
    if (uft_value >= group_min && uft_value <= group_max) {
        best_group = i;
        break;  ← 精确匹配，优先级最高
    }
    /* 情况2：值小于分组最小值，扩展该分组 */
    else if (uft_value < group_min) {
        best_group = i;
        break;  ← 选择第一个超出的分组
    }
    /* 情况3：都不符合，默认最后一个分组 */
    else if (i == group_count - 1) {
        best_group = i;
    }
}
```

### 典型样本分配示例

```
当前分组状态：
├─ Group-0: [100, 1346]     → 状态ID=0
├─ Group-1: [1346, 4928]    → 状态ID=1
├─ Group-2: [4928, 18000]   → 状态ID=2
└─ group_count = 3

新样本到达处理：

┌──────────────────────────────────────────────────────────┐
│ UFT=600 (冷数据)                                         │
├──────────────────────────────────────────────────────────┤
│ Loop i=0:                                               │
│   group_min=100, group_max=1346                         │
│   Check: 600 >= 100 && 600 <= 1346? YES                │
│   ⟹ best_group = 0                                     │
│   ⟹ break                                              │
│ Result: 分配到Group-0，返回状态ID=0                     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ UFT=3000 (中温数据)                                      │
├──────────────────────────────────────────────────────────┤
│ Loop i=0:                                               │
│   group_min=100, group_max=1346                         │
│   Check: 3000 >= 100 && 3000 <= 1346? NO               │
│   Check: 3000 < 100? NO                                │
│   Continue...                                           │
│ Loop i=1:                                               │
│   group_min=1346, group_max=4928                        │
│   Check: 3000 >= 1346 && 3000 <= 4928? YES             │
│   ⟹ best_group = 1                                     │
│   ⟹ break                                              │
│ Result: 分配到Group-1，返回状态ID=1                     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ UFT=50 (超冷数据，小于最小分组)                          │
├──────────────────────────────────────────────────────────┤
│ Loop i=0:                                               │
│   group_min=100, group_max=1346                         │
│   Check: 50 >= 100 && 50 <= 1346? NO                   │
│   Check: 50 < 100? YES ← 找到扩展点                    │
│   ⟹ best_group = 0                                     │
│   ⟹ break                                              │
│ Update: Group-0.min_val = 50 (扩展最小值)              │
│ Result: 分配到Group-0，返回状态ID=0                     │
│ Side Effect: Group-0范围扩展到[50, 1346]               │
└──────────────────────────────────────────────────────────┘
```

---

## 第六部分：Q-Learning状态空间演化

### 在Q-Table中的映射

```
┌─────────────────────────────────────────────────────────┐
│ 状态空间演化与Q-Table覆盖率                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 初期（Sample 1~99）：                                 │
│   ├─ Active States: 1 (Group-0)                      │
│   ├─ Q-Table覆盖率: 1/64 ≈ 1.6%                      │
│   ├─ 所有GC操作都学习到Q-Table[*][0]                 │
│   └─ 问题：无法区分不同温度数据的策略                │
│                                                         │
│ 分裂期（Sample 100~5000）：                           │
│   ├─ Active States: 1 → 64 (指数增长)               │
│   ├─ Q-Table覆盖率: 1/64 → 100%                      │
│   ├─ 分组粒度不断细化                                 │
│   ├─ 每个新分组初始Q值 = 0                           │
│   └─ 优点：为冷/热数据学习差异化策略                 │
│                                                         │
│ 稳定期（Sample 5000~50000）：                        │
│   ├─ Active States: 64 (饱和)                        │
│   ├─ Q-Table覆盖率: 100%                             │
│   ├─ 所有分组已激活                                   │
│   ├─ 状态 ↔ 分组 一一对应                            │
│   └─ 聚焦：精细化Q值学习                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 具体示例：5页受害块处理中的分组演化

```
时间线：GC #2500（距训练开始2500次GC循环）

┌─────────────────────────────────────────────────────────┐
│ 当前分组状态（已有约15个分组）                          │
├─────────────────────────────────────────────────────────┤
│ Group-0:  [50, 200]        → 超冷数据                  │
│ Group-1:  [200, 600]       → 冷数据                    │
│ Group-2:  [600, 1800]      → 中冷数据                  │
│ Group-3:  [1800, 5400]     → 中温数据                  │
│ Group-4:  [5400, 10000]    → 中热数据                  │
│ ...                                                     │
│ Group-14: [100000, 500000] → 超热数据                 │
└─────────────────────────────────────────────────────────┘

处理5页受害块：

Page 1: UFT=800  → Group-2 (状态ID=2)
        ├─ 现状：Q[2][HOT]=100, Q[2][MEDIUM]=50, Q[2][COLD]=-20, Q[2][SUPER]=10
        ├─ 选择：argmax = HOT (Q值最高)
        ├─ 分配：HOT块
        └─ 记录：(state=2, action=HOT, block_id=X)

Page 2: UFT=45000 → Group-13 (状态ID=13)
        ├─ 现状：Q[13][HOT]=-100, Q[13][MEDIUM]=-30, Q[13][COLD]=20, Q[13][SUPER]=-50
        ├─ 选择：argmax = COLD (Q值最高)
        ├─ 分配：COLD块
        └─ 记录：(state=13, action=COLD, block_id=Y)

Page 3: UFT=5500 → Group-4 (状态ID=4)
        ├─ 现状：Q[4][HOT]=200, Q[4][MEDIUM]=180, Q[4][COLD]=-100, Q[4][SUPER]=150
        ├─ 选择：argmax = HOT (Q值最高)
        ├─ 分配：HOT块
        └─ 记录：(state=4, action=HOT, block_id=Z)

页面3处理完后（积累reward）：
  ├─ 总reward = (削除页数 - 迁移页数) * 权重 = 正值
  ├─ 分布：均分到3条hash table记录
  └─ Q值更新：
       Q[2][HOT] = 100 + 1*(reward/3 - 100) = 100 + α*delta
       Q[13][COLD] = 20 + 1*(reward/3 - 20)
       Q[4][HOT] = 200 + 1*(reward/3 - 200)
```

---

## 第七部分：分裂算法的关键特性

### 1. 适应性（Adaptivity）

```
特点：根据实际UFT值分布自动调整分组

示例对比：
┌────────────────────────────────────────────┐
│ 固定分组方案（旧方法）                     │
├────────────────────────────────────────────┤
│ Group-0: [0, 2500]                        │
│ Group-1: [2500, 5000]                     │
│ Group-2: [5000, 7500]                     │
│ Group-3: [7500, 10000]                    │
│                                            │
│ 问题：                                    │
│ ├─ 如果大部分UFT在0~1000，其他分组浪费  │
│ └─ 无法应对超出10000的数据                │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ 自适应分组方案（新方法）                   │
├────────────────────────────────────────────┤
│ Group-0: [50, 500]      (样本多，细分)   │
│ Group-1: [500, 3000]    (样本多，细分)   │
│ Group-2: [3000, 50000]  (样本少，粗分)   │
│ Group-3: [50000, ∞]     (稀疏数据)       │
│                                            │
│ 优点：                                    │
│ ├─ 高频UFT范围精细化                     │
│ ├─ 低频UFT合并处理                       │
│ └─ 能处理任意UFT值范围                   │
└────────────────────────────────────────────┘
```

### 2. 几何分裂（Geometric Partitioning）

```
为什么使用几何平均而非算术平均？

数据特征：UFT分布通常是对数正态或指数分布

示例：
  min_val = 100, max_val = 100000
  
  算术平均分裂：
    split = (100 + 100000) / 2 = 50050
    结果：[100, 50050] 和 [50050, 100000]
    问题：分组范围严重不均衡 (49950 vs 49950在表面，但对数跨度不同)
  
  几何平均分裂：
    split = sqrt(100 * 100000) = sqrt(10000000) ≈ 3162
    结果：[100, 3162] 和 [3162, 100000]
    优势：
      ├─ log(3162) - log(100) ≈ 5.04
      ├─ log(100000) - log(3162) ≈ 5.06
      └─ 对数空间均衡分割 ✓

优势总结：
  ✓ 更符合数据的指数分布特性
  ✓ 避免极值偏差
  ✓ 均衡的Q-Learning学习压力
```

### 3. 饱和机制（Saturation）

```
特点：最多64个分组，避免无限增长

饱和曲线：
┌─────────────────────────────────────────┐
│  group_count                            │
│         /                               │
│      64|─────────────────── (饱和)      │
│       /|                               │
│     /  |                               │
│    /   |                               │
│   /    |                               │
│  /     |                               │
│ 1      └─────────────────────────      │
│        0   5000  20000  50000  GC循环   │
│           ↑             ↑              │
│       快速增长          饱和           │
└─────────────────────────────────────────┘

达到64个分组后：
  ├─ 新样本仍被分配到最接近的分组
  ├─ 分组范围会进一步扩展
  ├─ 可能出现 max_val >= 2*min_val，但无法继续分裂
  └─ 这是可接受的（权衡：精度 vs 内存/计算成本）
```

---

## 第八部分：分组演化的性能影响

### 1. 状态识别精度

```
┌─────────────────────────────────────────────────┐
│ 状态识别精度随分组数增加而提升                   │
├─────────────────────────────────────────────────┤
│ 分组数 │ 精度等级 │ 对应Q-Learning学习效果    │
├─────────────────────────────────────────────────┤
│ 1     │ ★☆☆☆☆ │ 无法区分数据，学习无效    │
│ 2-4   │ ★★☆☆☆ │ 基本区分冷/热，初步学习  │
│ 4-8   │ ★★★☆☆ │ 较好的粒度，Q值收敛     │
│ 8-16  │ ★★★★☆ │ 细粒度控制，策略优化     │
│ 16-32 │ ★★★★★ │ 高精度识别，最优策略     │
│ 32-64 │ ★★★★★ │ 超细粒度，饱和收益       │
└─────────────────────────────────────────────────┘
```

### 2. 计算复杂度

```
时间复杂度分析：

每次离散化操作：
  ├─ 线性查找：O(group_count)
  ├─ 分裂判断：O(1)
  ├─ 范围更新：O(1)
  └─ 总耗时：O(group_count) ≈ O(log n) 摊销

空间复杂度：
  ├─ 分组数组：O(64)（固定上限）
  ├─ Q-Table：O(64 * 4) = O(256)（固定）
  ├─ Hash表：O(页面数)（变动）
  └─ 总空间：O(1) + O(哈希表)
```

### 3. 收敛特性

```
Q值收敛过程：

┌──────────────────────────────────────────────────┐
│ Q值收敛曲线（每个状态-动作对）                   │
├──────────────────────────────────────────────────┤
│  Q值                                             │
│    |     ╭─ 最优策略                            │
│  σ |   ╱╲                                       │
│    | ╱   ╲    ╭── 新分组创建时的跳跃             │
│  μ |╱     ╲╭─╯╲    ╱╲                          │
│    |       ╲  ╱╲__╱  ╲╭─                       │
│  0 |────────╲╱────╲──╱─╲───────────────         │
│    └────────────────────────────────────→ GC循环│
│     冷启动   单分组   分裂期   稳定期            │
│                                                 │
│ 特征：                                         │
│ ├─ 冷启动：Q=0，高方差                        │
│ ├─ 积累期：Q缓慢增长                          │
│ ├─ 分裂期：新分组Q跳跃（重新初始化）           │
│ └─ 稳定期：Q向最优值收敛，方差降低            │
└──────────────────────────────────────────────────┘
```

---

## 总结：分组策略的演化节点

```
┌─────────────────────────────────────────────────────────┐
│ 分组策略演化的6个关键节点                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 节点1：初始化（Sample 1）                              │
│   └─ 行为：创建第一个分组 [uft, uft]                  │
│   └─ 状态空间：1/64 (1.6%)                            │
│                                                         │
│ 节点2：样本积累（Sample 2-99）                        │
│   └─ 行为：扩展分组范围                               │
│   └─ 状态空间：1/64 (不变)                           │
│                                                         │
│ 节点3：第一次分裂（Sample 100）                       │
│   └─ 行为：分组从1变为2                               │
│   └─ 状态空间：2/64 (3.1%)                           │
│   └─ 触发：sample_count ≥ 100 ∧ max ≥ 2*min        │
│                                                         │
│ 节点4：快速分裂期（Sample 100-5000）                 │
│   └─ 行为：分裂呈指数增长                             │
│   └─ 状态空间：2 → 32 (3.1% → 50%)                 │
│   └─ 频率：每100~200个样本一次分裂                   │
│                                                         │
│ 节点5：缓速分裂期（Sample 5000-20000）               │
│   └─ 行为：分裂速度减缓                               │
│   └─ 状态空间：32 → 50 (50% → 78%)                 │
│   └─ 原因：越来越多分组满足 max < 2*min 条件         │
│                                                         │
│ 节点6：饱和稳定期（Sample 20000-50000）              │
│   └─ 行为：分组维持在64个                             │
│   └─ 状态空间：64/64 (100%)，不再增长               │
│   └─ 焦点：Q值学习与优化（而非分裂）               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 应用意义

### 1. 为什么需要分组进化？

```
✓ 自适应状态空间设计
  └─ 而不是固定死板的日志映射

✓ 充分利用Q-Table的64个状态
  └─ 初期：1个状态学习全局策略（泛化）
  └─ 中期：多个状态学习分化策略（专化）
  └─ 后期：64个状态学习精细策略（优化）

✓ 处理长尾分布
  └─ 自动为高频数据细分，低频数据合并

✓ 平衡精度与成本
  └─ 最多64个分组 → 内存恒定
  └─ 分裂可选 → 计算可控
```

### 2. 与强化学习的配合

```
GC处理流程中的分组作用：

  受害块选择 → 有效页提取 → 【分组→获取状态ID】
                              ↓
                        Q表查询：Q[state_id][action]
                              ↓
                        选择最优动作 (ε-greedy)
                              ↓
                        执行分配策略
                              ↓
                        【后续收集reward】→ 【批量更新Q[state_id][action]】

分组的关键价值：
  ├─ 提供"状态特征向量"（UFT→状态ID）
  ├─ 使不同热度数据学到不同策略
  └─ 随GC循环数递进演化，逐步优化
```

